const mongoose = require("mongoose");
const { Schema } = require("mongoose");
const validator = require("validator");

const mokhaSchema = new Schema({
  
});

module.exports = mongoose.model("Mokha", mokhaSchema);